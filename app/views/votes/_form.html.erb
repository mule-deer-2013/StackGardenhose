<div>

  <div>
    <%= render partial: "votes/counter", locals: {votable: votable} %> 
  </div>

  <%= form_for @vote, remote: true do |f| %>
    <%= f.hidden_field :value, :value => 1 %>
    <%= f.hidden_field :votable_id, :value => votable.id %>
    <%= f.hidden_field :votable_type, :value => votable.class %>
    <%= f.submit "Upvote" %>
  <% end %>

  <%= form_for @vote, remote: true do |f| %>  
    <%= f.hidden_field :value, :value => -1 %>
    <%= f.hidden_field :votable_id, :value => votable.id %>
    <%= f.hidden_field :votable_type, :value => votable.class %>
    <%= f.submit "Downvote" %>
  <% end %> 

</div>

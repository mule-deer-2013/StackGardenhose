
<li><%= answer.body %> | <%= render partial: "votes/form", locals: {votable: answer } %>

    <% if current_user.id == answer.user_id %>
      <%= link_to "Edit Your Answer", edit_question_answer_path(@question, answer) %>
    <%end %>

</li>
